#include <stdio.h> //HACK: Using stdio for printing

int main(int argc, const char *argv[]) {
  for(int i = 0; i < 5; i++) { //NOTE: From 0 to 5
    printf("%d,\n", i); //FIXME: Remove last comma
  }
  //TODO: Count also to 9
  return -1; //BUG: Do not returns 0
}
