#include <stdio.h> //HACK: Using stdio for printing

int main(int argc, const char *argv[]) {
  for(int i = 0; i <= 5; i++) { //NOTE: From 0 to 5 inclusive
    printf("%d,\n", i); //FIXME: Remove last comma
  }
  //TODO: Also count up to 9
  return -1; //BUG: Non-zero value to indicate error
}
